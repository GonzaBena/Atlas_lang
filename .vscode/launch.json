{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/atlas_lang",
            "args": ["./example/integers.atlas"],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}